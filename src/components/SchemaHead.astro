---
const { items = [] } = Astro.props;

const validItems = Array.isArray(items)
  ? items.filter((i) => typeof i === "object" && i !== null)
  : [];
---

{
  validItems.map((obj) => (
    <script
      is:inline
      set:html={JSON.stringify(obj)}
      type="application/ld+json"
    />
  ))
}
